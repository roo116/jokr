<div id="saved-joke-container">
  {{#if savedJokes.length}}
  <h3>Saved Jokes:</h3>
    {{#each savedJokes}}
      {{!-- saved joke cards go here --}}
    {{/each}}
    </ul>
  </div>
  {{else}}
  <p>You have no saved jokes yet!</p>
  {{/if}}
</div>